'use strict';

module.exports = {
    connectQueue   : require('./connectQueue'),
    createPublisher: require('./createPublisher'),
    createQueue    : require('./createQueue'),
    getChannel     : require('./getChannel'),
    publishMessage : require('./publishMessage')
};